""" Main server class """

# Import modules
from sys import stdout stdout, stderr, path
from os import name, path, listdir, getcwd
from socket import socket, AF_INET, AF_INET6, SOCK_STREAM
from select import select
from logging import getLogger, FileHandler, StreamHandler, DEBUG, INFO, WARNING, ERROR
from time import sleep, perf_conf

if __name__ == "__main__":
    print("This file is not main")
    exit()

